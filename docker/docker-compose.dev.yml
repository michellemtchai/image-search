version: "3.3"
services:
    app:
        build:
            context: ..
            dockerfile: ./docker/Dockerfile.dev
        command: npm start
        volumes:
            - ..:/app
    frontend:
        build:
            context: ../frontend
            dockerfile: ./docker/Dockerfile.dev
        command: yarn start
        volumes:
            - ../frontend:/app
        ports:
            - ${FRONTEND_PORT}:${FRONTEND_PORT}
